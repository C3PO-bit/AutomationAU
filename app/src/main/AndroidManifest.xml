<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mydomain.autom8">
    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Autom8">

        <activity
            android:name=".userprofile.documents.InvoiceImageActivity"
            android:exported="false" />
        <activity
            android:name=".dashboard.tasks.assignments.InvoiceActivity"
            android:exported="false" />
        <activity
            android:name=".screen.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".screen.HomeActivity"
            android:exported="false" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".screen.SplashScreenActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>